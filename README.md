# CNN-Cat-vs-Dog# Cat vs Dog — Flask deployment


Place your trained model file `cat_vs_dog_cnn_model.pth` at the repo root (next to `app.py`).


### Run locally (virtualenv)


```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
# open http://localhost:7860